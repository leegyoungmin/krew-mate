---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: agentmessages.krewmate.io
spec:
  group: krewmate.io
  names:
    kind: AgentMessage
    listKind: AgentMessageList
    plural: agentmessages
    shortNames:
    - amsg
    singular: agentmessage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.from
      name: From
      type: string
    - jsonPath: .spec.to
      name: To
      type: string
    - jsonPath: .status.delivered
      name: Delivered
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              actionHint:
                type: string
              content:
                type: string
              from:
                type: string
              teamRef:
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              to:
                type: string
            required:
            - content
            - from
            - teamRef
            - to
            type: object
          status:
            properties:
              delivered:
                type: boolean
              deliveredAt:
                format: date-time
                type: string
              readAt:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
